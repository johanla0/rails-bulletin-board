first_image_attachment:
  name: image
  record: one (Bulletin)
  blob: first_image_blob

second_image_attachment:
  name: image
  record: two (Bulletin)
  blob: second_image_blob

draft_attachment:
  name: image
  record: draft (Bulletin)
  blob: <%= %w[first_image_blob second_image_blob].sample %>

under_moderation_attachment:
  name: image
  record: under_moderation (Bulletin)
  blob: <%= %w[first_image_blob second_image_blob].sample %>

published_attachment:
  name: image
  record: published (Bulletin)
  blob: <%= %w[first_image_blob second_image_blob].sample %>

rejected_attachment:
  name: image
  record: rejected (Bulletin)
  blob: <%= %w[first_image_blob second_image_blob].sample %>

archived_attachment:
  name: image
  record: archived (Bulletin)
  blob: <%= %w[first_image_blob second_image_blob].sample %>
